@model IEnumerable<zBartender.Models.Queue>

@{
    ViewBag.Title = "Queue";
}

<h2>Queue</h2>

<p>
</p>
<table class="table">
    <tr>
        <th>
            Drink Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Remove", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
